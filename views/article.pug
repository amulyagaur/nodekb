extends layout

block content
    h1=article.title
    h3 Author: #{author}
    p=article.body
    hr
    if user
        if user._id == article.author
            a.btn.btn-warning(href='/articles/edit/'+article._id) Edit
            
